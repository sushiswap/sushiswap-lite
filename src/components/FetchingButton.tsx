import React from "react";

import Button from "./Button";

const FetchingButton = () => {
    return <Button size={"large"} title={"Fetching Price…"} disabled={true} />;
};
export default FetchingButton;
